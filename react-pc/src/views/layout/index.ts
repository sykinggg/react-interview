import { asyncComponentArr as layoutGrid } from './layoutGrid/components';
import { asyncComponentArr as layoutStructure } from './layoutStructure/components';

export const layoutAntdComponent = layoutGrid.concat(layoutStructure);
